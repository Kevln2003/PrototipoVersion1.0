<Page
    x:Class="PrototipoVersion1._0.Visual.Plantilla.Busqueda"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrototipoVersion1._0.Visual.Plantilla"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Campos de búsqueda -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Buscar Paciente" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Nombre:" FontSize="16" Margin="0,10,0,0"/>
            <TextBox x:Name="NombreTextBox" PlaceholderText="Ingrese el nombre" Margin="0,0,0,10"/>

            <TextBlock Text="Apellido:" FontSize="16" Margin="0,10,0,0"/>
            <TextBox x:Name="ApellidoTextBox" PlaceholderText="Ingrese el apellido" Margin="0,0,0,10"/>

            <TextBlock Text="Cédula:" FontSize="16" Margin="0,10,0,0"/>
            <TextBox x:Name="CedulaTextBox" PlaceholderText="Ingrese la cédula" Margin="0,0,0,10"/>

            <Button Content="Buscar" Background="Blue" Foreground="White" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Resultados de búsqueda -->
        <StackPanel Grid.Row="1" Margin="0,20,0,0">
            <TextBlock Text="Resultados:" FontSize="18" FontWeight="Bold"/>
            <ListView x:Name="ResultadosListView" Margin="0,10,0,0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Nombre}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Apellido}" />
                            <TextBlock Text="{Binding Cedula}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
